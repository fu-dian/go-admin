# fresh.conf
# 编译命令：添加 -buildvcs=false 关闭VCS校验
build_cmd = go build -buildvcs=false -o tmp/{{.AppName}} {{.Path}}
# 监听的文件类型（保持默认）
watch_ext = .go, .html, .toml
# 忽略的目录（保持默认）
ignore_dir = vendor, tmp, node_modules